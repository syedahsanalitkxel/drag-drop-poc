import React, { Component, Fragment } from 'react';
import Editcomponent from '../AddClient';

const EditClient: React.FunctionComponent<any> = ({
  assessments,
  filterAssessments,
  add,
  edit,
  remove,
}) => {
  return <Editcomponent />;
};

export default EditClient;
